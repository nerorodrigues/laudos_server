directive @isLoggedIn(role: String, schema: String) on OBJECT | FIELD_DEFINITION | SCHEMA

enum ExamsStatus{
    WAITING,
    READY
}

# enum ROLES{
#     ADMIN
#     SYSTEM_ADMIN,
#     USER
# }

# type AuthUser{
#     token: String!
# }

# input UserInput {
#     email: String!
#     nome: String!
# }

# type User{
#     userName: String!
#     role: ROLES!
# }
type Subscription{
    test(id: ID) : String
}

type Mutation{
    testMutation: String
}

type Query{
    test: String
}