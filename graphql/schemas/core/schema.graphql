directive @isLoggedIn(role: String, schema: String) on OBJECT | FIELD_DEFINITION | SCHEMA

enum ExamsStatus{
    WAITING,
    READY
}

type Subscription{
    test(id: ID) : String
}

type Mutation{
    testMutation: String
}

type Query{
    test: String
}