directive @isLoggedIn(role: ROLES) on OBJECT | FIELD_DEFINITION | SCHEMA

enum ROLES{
    ADMIN
    SYSTEM_ADMIN,
    USER
}

type AuthUser{
    token: String!
}

input UserInput {
    email: String!
    nome: String!
}

type User{
    userName: String!
    role: ROLES!
}
